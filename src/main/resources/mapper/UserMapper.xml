<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.everyChallenge.mapper.UserMapper">

	<select id="findByLoginId" parameterType="string" resultType="com.everyChallenge.domain.User">
		SELECT *
		FROM user
		WHERE login_id = #{loginId} AND is_deleted = 0
	</select>
	
	<insert id="insertUser">
		INSERT INTO user (login_id, password, user_name) 
		VALUES (#{loginId}, #{password}, #{userName})
	</insert>

</mapper>